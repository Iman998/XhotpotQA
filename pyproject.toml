[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "xhotpotqa"
version = "0.4.1"
description = "Construction and evaluation for cross-lingual multi-hop QA over mixed-language evidence"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
  {name = "Iman Barati", email = "iman_barati@comp.iust.ac.ir"},
  {name = "Arash Ghafouri"},
  {name = "Behrouz Minaei-Bidgoli"},
]
keywords = ["cross-lingual", "multi-hop question answering", "benchmark", "HotpotQA"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = ["PyYAML>=6.0"]

[project.urls]
Paper = "https://arxiv.org/abs/2608.27481"
Repository = "https://github.com/Iman998/XhotpotQA"
Dataset = "https://huggingface.co/datasets/Iman998/XhotpotQA"

[project.optional-dependencies]
generation = ["openai>=1.99"]
legacy = ["ijson>=3.3"]
release = ["datasets>=3.6", "huggingface-hub>=0.33", "ijson>=3.3", "pyarrow>=18"]
dev = [
  "build>=1.2",
  "mypy>=1.15",
  "pre-commit>=4.0",
  "pytest>=8.3",
  "pytest-cov>=6.0",
  "ruff>=0.11",
  "types-PyYAML>=6.0",
]

[project.scripts]
xhotpotqa = "xhotpotqa.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/xhotpotqa"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.mypy]
python_version = "3.10"
strict = true
packages = ["xhotpotqa"]
mypy_path = "src"
explicit_package_bases = true
no_site_packages = true

[[tool.mypy.overrides]]
module = ["yaml", "openai", "huggingface_hub", "ijson"]
ignore_missing_imports = true

[tool.pytest.ini_options]
addopts = "-q --strict-config --strict-markers"
pythonpath = ["src"]
testpaths = ["tests"]
xfail_strict = true

[tool.coverage.run]
branch = true
source = ["xhotpotqa"]

[tool.coverage.report]
show_missing = true
skip_covered = true
